# Mission for set1 (simple square room)
# Bounded rectangle in the (X, Y) plane per assignment spec.
start 25 25 5
min_x   0
max_x 100
min_y   0
max_y 100
height_min 0
height_max 10
# Mapping resolution: number of decimal places after the dot in meter-based
# output (cell_size_cm = 100 / 10^N). N=1 -> 10 cm cells, matching the map.
xy_resolution     1
height_resolution 1
