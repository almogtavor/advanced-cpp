# Drone capabilities for set1 (small open room)
# All lengths in cm, angles in degrees.
min_passage_width   30
min_passage_length  30
min_passage_height  50
# Lidar geometry per the assignment specification:
#   Z-min: minimum measurable distance
#   Z-max: maximum range
#   D:     spacing between consecutive beam circles, measured at Z-min
#   FOVC:  number of beam circles (Circle 0 is the central beam)
lidar_z_min          5
lidar_z_max        200
lidar_d              2
lidar_fovc           4
max_rotate_per_cmd 180
max_advance_per_cmd 100
max_elevate_per_cmd 100
