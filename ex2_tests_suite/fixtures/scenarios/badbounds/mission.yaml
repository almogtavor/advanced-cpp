mission_config:
  max_steps: 6000
  boundaries:
    x_boundary: { min_cm: 100, max_cm: 0 }
    y_boundary: { min_cm: 0, max_cm: 100 }
    height_boundary: { min_cm: 20, max_cm: 50 }
  gps_resolution_cm: 10
  output_mapping_resolution_factor: 1
