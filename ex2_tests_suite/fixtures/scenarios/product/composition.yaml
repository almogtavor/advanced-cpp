simulation_compositions:
  simulations:
    - simulation_config: "sim.yaml"
      mission_configs: ["mission.yaml"]
  drone_configs: ["drone_small.yaml", "drone_large.yaml"]
  lidar_configs: ["lidar_a.yaml", "lidar_b.yaml"]
